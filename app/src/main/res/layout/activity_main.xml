<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/degree_button"
        android:layout_width="wrap_content"
        android:layout_height="100dp"
        android:layout_marginTop="300dp"
        android:text="CONVERT RADIAN TO DEGREE"
        android:textSize="20sp"
        android:background="@color/white"
        app:layout_constraintBottom_toTopOf="@+id/radian_button"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.5" />

    <Button
        android:id="@+id/radian_button"
        android:layout_width="wrap_content"
        android:layout_height="100dp"
        android:text="CONVERT DEGREE TO RADIAN"
        android:textSize="20sp"
        android:background="@color/black"
        android:layout_marginTop="10dp"
        app:layout_constraintEnd_toEndOf="@+id/degree_button"
        app:layout_constraintStart_toStartOf="@+id/degree_button"
        app:layout_constraintTop_toBottomOf="@+id/degree_button" />

</androidx.constraintlayout.widget.ConstraintLayout>